import Component from '@glimmer/component';
import ApplicationInstance from '@ember/application/instance';

export default class MoveNetComponent extends Component {
  constructor(owner: ApplicationInstance, args: any) {
    super(owner, args);
  }
}
